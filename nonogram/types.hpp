#ifndef ___TYPES_IMPLEMENTATION___
#define ___TYPES_IMPLEMENTATION___

#include <vector>
#include <tuple>

using axis_t = std::vector<std::vector<int>>;
using clue_t = std::pair<axis_t, axis_t>;
using board_t = std::vector<std::vector<bool>>;

#endif
